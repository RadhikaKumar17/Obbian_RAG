# Deployment status

Not deployed yet. Target: a new Railway service named `obbian-rag` with a `/data` volume.

Required next inputs: Railway project access and `RAG_GROQ_API_KEY`. No frontend or backend integration has been performed.
