services:
  rag:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    environment:
      RAG_INDEX_DIR: /data/index
    volumes:
      - rag-index:/data/index
    restart: unless-stopped
volumes:
  rag-index:
